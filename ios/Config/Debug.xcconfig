// Debug builds talk to the Python API running locally
// (cd api && uv run uvicorn app.main:app --port 8000).
// On a phone, replace localhost with your Mac's LAN address, or point this at
// the deployed service.
API_BASE_URL = http:/$()/localhost:8000
