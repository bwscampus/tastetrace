import XCTest
@testable import TasteTrace

// App-level tests run in the simulator; the package tests cover the logic.
final class TasteTraceAppTests: XCTestCase {
    func testAppTargetLinks() {
        XCTAssertTrue(true)
    }
}
